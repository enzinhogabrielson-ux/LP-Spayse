kind = "web"
previewPath = "/"
title = "XSCALES - Site Institucional"
version = "1.0.0"
id = "artifacts/xscales-site"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 3000

[services.development]
run = "pnpm --filter @workspace/xscales-site run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/xscales-site", "run", "build" ]
publicDir = "artifacts/xscales-site/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "3000"
BASE_PATH = "/"
